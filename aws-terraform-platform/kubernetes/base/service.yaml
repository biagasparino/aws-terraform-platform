apiVersion: v1
kind: Service
metadata:
  name: api-1
spec:
  selector: { app: api-1 }
  ports:
    - port: 80
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: api-2
spec:
  selector: { app: api-2 }
  ports:
    - port: 80
      targetPort: 3000
